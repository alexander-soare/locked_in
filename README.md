Alex's setup for locking in.

Just run `bash setup.sh`

### Brostrend Wifi antenna

This has it's own script: `install_brostrend_wifi_antenna.sh`. Run it with `sudo`. Last worked on Ubuntu 24 on April 9 2026.